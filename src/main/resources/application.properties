spring.application.name=SIGMA
#server.servlet.context-path=/sigma

server.port=8090
spring.profiles.active=mail, audit, dev-db, jwt
#, livereload

front.adress=http://localhost:3000
auth.activate-account-link=/#/authentication/active-acount
auth.reinit-password-link=/#/authentication/forget-password

# Multipart upload configuration (increase limits to allow larger files)
# Adjust these values according to your infrastructure policies.
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=60MB
# Write files larger than this threshold directly to disk
spring.servlet.multipart.file-size-threshold=2MB
