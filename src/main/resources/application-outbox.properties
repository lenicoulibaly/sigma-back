## Outbox dispatcher configuration
# Polling delay between batches in milliseconds
outbox.dispatch.fixed-delay=1000

# Maximum number of events processed per batch
outbox.dispatch.batch-size=100

# Maximum retry attempts before moving an event to DEAD state
outbox.dispatch.max-attempts=5
